# 插件 Bug 审查台账

> 开始时间：2026-08-29
> 范围：`humanize/`（83 个 .py）、`webui/`（29 个 .js）、`main.py`，合计约 3.5 万行
> 方法：AST 静态扫描（变量遮蔽 / 异常吞没 / 可变默认参数 / 可疑索引取值）
>       + 人工复核 + 最小复现脚本验证
> 规则：**查到一个、记录一个、修一个、commit 一次**
> 状态：`[未开始]` `[排查中]` `[已确认]` `[已修复]` `[非缺陷]` `[风险]`

## 统计

| 项 | 数量 |
|---|---|
| AST 扫描初筛 | 47 |
| 人工复核后确认缺陷 | 见下表 |
| 已修复并 commit | 见下表 |
| 非缺陷（良性遮蔽 / 误报） | 见下表 |

---

## 一、已确认并修复

| # | 位置 | 类型 | 摘要 | Commit |
|---|---|---|---|---|
| A-01 | `humanize/repositories/jargon.py` `list_jargons` | LIKE 通配符未转义 | 搜索含 `_`/`%` 的词条会误匹配其它词条，接口把错误详情返回前端 | `21b41bb` |
| A-02 | `humanize/image_cache.py` `store()` | 契约违背（失败仍报成功） | 索引写失败仍返回 `cached=True`，产生永不回收的无主文件 | `d966b56` |
| A-03 | `main.py:109` `_CONTROL_TAG_PATTERN` | 防御缺口（正则漏匹配） | 漏掉 `<Messages>` 容器标签，含协议标签的文本可绕过 fail-closed 校验被直接发出 | `429de73` |
| A-04 | `main.py:1626` 最终快照持久化 | 裸 `create_task` 丢引用 | 任务无强引用可被 GC 中途回收，最终快照静默丢失；异常无人接收 | `acf952b` |
| A-05 | `humanize/memory.py:165` `ChatMemoryService.initialize` | 阻塞调用跑在事件循环线程 | `async def initialize` 直接调用 workspace `initialize()`（文件锁 + `time.sleep` 轮询，超时 5s）；与 worker 线程 `commit_turn` 争锁时阻塞整个事件循环 | `8702e1c` |
| A-06 | `humanize/jargon/normalizer.py:70` `term_matches` | 词边界误伤：`\w` 含 CJK | smart 模式 `(?<!\w)term(?!\w)` 中 `\w` 默认匹配汉字，英文术语紧邻中文即失配（如"求gpt-4链接"）；`is_valid_candidate` 同受影响，未知词候选被误滤 | 待 commit |

### A-01 词条创建回查时 LIKE 通配符未转义，可返回错误词条

**状态**：`[已修复]` — commit `21b41bb`

**位置**：
- `humanize/repositories/jargon.py` `list_jargons()`（参数拼入 `%{search}%`）
- `humanize/web/routes.py:683-698`（`create_entry` 后回查取 `[0]`）

**现象**：
创建词条后，接口用 `search=term` 模糊回查、取 `items[0]`，再据此取详情返回前端。
`search` 被拼成 `%{search}%` 直接进 `LIKE`，而 `%`、`_`、`\` **均未转义**，也没有
`ESCAPE` 子句。当 term 本身含 `_` 时，`_` 在 LIKE 中是"任意单字符"，会匹配到**别的**词条。

**放大因素**：
`jargon.py:733` 其实已经拿到 `new_entry_id = int(cursor.lastrowid)`，但 769 行只
`return True` 把它丢了，才退化成模糊回查。

**复现**（`git stash` 撤修复后实测，证明测试真能抓到）：
```
回查 search='a_b' 命中 2 条：id=2 'axb'、id=1 'a_b'
routes.py 取 [0] 会拿到 : 'axb'      ← 错
AssertionError: wildcard leaked into the match: ['a_b', 'axb']
```
修复后命中 1 条，取到 `a_b`。

**旁证**：`repositories/reply_examples.py:57-67` 同样的搜索功能**已经**正确转义
（`escaped = search.replace(...)` + `ESCAPE '\'`），说明这是实现不一致而非有意设计。
修复沿用该既有模式，保持两处写法一致。

---

### A-02 图片缓存索引写失败后仍上报 cached=True，产生无主文件

**状态**：`[已修复]` — commit `d966b56`

**位置**：`humanize/image_cache.py:96-98` `ImageCacheStore.store()`

**现象**：
`store()` 的 docstring 明确承诺"任何失败都返回 `cached=False` 让调用方 fail open"。
但索引分支（写 `humanize_image_cache` 表）失败时只 `logger.exception`，随后仍
`return CachedImage(file_path=<缓存路径>, cached=True)`。

**后果链**：
1. 文件已落盘，但 DB 无对应行；
2. `_evict()`（110-130 行）回收磁盘**完全依赖** DB 记录
   （`list_image_cache_entries()` → 只删它看得见的文件）；
   → 该文件永远不被清理，**磁盘泄漏**；
3. `main.py:278` 见 `cached=True` 就把消息组件的路径改写到这个无主文件上。

**为什么只改契约、不删文件**：
同 hash 图片的文件名相同（`f"{digest}{suffix}"`）且 `_store_sync` 幂等不覆写，
多条消息可能指向同一文件。索引失败时直接 unlink 会误删其它正常记录的图片。
故只修正上报值，孤儿文件另记为风险 R-04。

**复现**（撤修复后）：
```
RuntimeError: image cache index unavailable
→ CachedImage(file_path='.../image_cache/2c8648d1....png', cached=True)   ← 错
```
修复后返回原始路径 + `cached=False`。新增 `tests/test_image_cache.py` 覆盖
成功 / 拷贝失败 / 索引失败 / 未启用四条路径。

---

### A-03 控制标签正则漏掉 `<Messages>` 容器，可绕过 fail-closed 校验

**状态**：`[已修复]` — commit `429de73`

**位置**：`main.py:109` `_CONTROL_TAG_PATTERN`

**现象**：
正则只列了单数 `Message`，且其尾部 lookahead `(?=\s|/?>|&gt;)` **拒绝** `<Messages>`
里的 `s` —— 于是 `<Messages>文本</Messages>` 匹配不到任何东西。

**为什么危险**：
该模式的 4 处使用**全部是 fail-closed 语义**（匹配=拦截，不匹配=放行）：
- `main.py:1781` 装饰后响应含控制标签 → 抑制 + 记失败
- `main.py:2883` 热重载闸门：含控制标签 → **不发**
- `main.py:3046` 热重载工具阶段：**不含**控制标签 → 直接 `_send_chain()` 放行（未经校验）
- `main.py:3156` 工具阶段响应含控制标签 → 抑制

因此在 3046/2883 的热重载路径上，漏匹配 = 未校验内容被原样发出。

**复现**（撤修复后实测，证明不是理论风险）：
```
assert ['改写正文<Messages>泄漏的容器标签</Messages>'] == []
AssertionError —— 泄漏内容被真的发送出去了
```

**修复**：在交替分支中加入 `Messages`（`Action|UnknownTerms|ImageCache|Messages|Reply|Message`）。
四处语义统一变严（更多拦截 / 更少放行），方向安全。

---

## 二、排查后判定为非缺陷（保留记录，避免重复排查）

| # | 位置 | 扫描告警 | 复核结论 |
|---|---|---|---|
| N-01 | `humanize/openviking/type_quota.py:369,404` | `memory_type` / `rank` / `origin` 遮蔽外层同名 | **良性**：循环内用的都是自身绑定；循环后改读 `entries` / `entry.origin`，不依赖外层残留值。但 374 行 `sort_key` 闭包捕获外层 `memory_type`，若将来把闭包移到循环外会取错值 → 记为**脆弱写法**（见 R-01） |
| N-02 | `humanize/context/window.py:359` | `context_ref` 遮蔽 | **良性且有意为之**：循环内匹配到 `turn_ref` 就 `return`，否则循环结束后第 368 行重新赋值为新 ref。逻辑正确 |
| N-03 | `humanize/openviking/adapter.py:495` | `commit_id` 遮蔽 | **良性**：第 638 行的 `commit_id` 位于 635–645 的**列表推导式**内，Python 3 推导式有独立作用域，用的是自己的迭代变量，不是泄漏值 |
| N-04 | `humanize/openviking/type_quota.py:361,391` | comprehension 变量遮蔽 | **误报**：Python 3  comprehension 有独立作用域，不会泄漏到外层 |
| N-05 | `humanize/image_cache.py:89,98` `memory.py:1253` `repositories/jargon.py:71` `repositories/sqlite.py:86` | `[0]` 索引取值 | 均为 `LIMIT 1` 或有前置存在性判断的查询，语义正确 |

## 三、风险项（暂不改，记录待评估）

| # | 位置 | 风险 |
|---|---|---|
| R-01 | `humanize/openviking/type_quota.py:374-381` | `sort_key` 闭包捕获外层循环变量 `memory_type`。当前正确（闭包在循环内定义并立即使用），但若重构时把闭包移出循环，将取到最后一次迭代的值 |
| R-02 | `humanize/web/routes.py:693` | 即便修了通配符，用"模糊搜索 + 取第一条"回查新创建对象仍比直接用 `lastrowid` 脆弱。根治需让 `apply_jargon_action` 传出新 ID，但会改动返回类型、影响 9 处测试断言 |
| R-03 | 19 处 `except ... : pass/continue` | 多为读取单个文件/解析单条记录的降级处理，符合"失败即跳过"设计。但 `provider_catalog.py:53,223` 与 `snapshots.py:403` 是 `except Exception: continue`，吞掉所有异常，排查问题时会静默。待评估是否至少加 debug 日志 |
| R-04 | `humanize/image_cache.py:96-103` | A-02 只修正了上报值，未删除落盘文件。索引写失败时该图片文件成为孤儿（`_evict()` 只按 DB 记录回收）。未一并删除是因为同 hash 文件被多条消息共享、且 `_store_sync` 幂等不覆写，直接 unlink 会误删其它正常记录。若需根治，应在删除前确认 DB 无该 hash 的任何行 |
| R-05 | `main.py:2883-2888`、`3044-3048` | 热重载竞态下（`_container is None`）对**不含控制标签**的普通文本放行，属有意权衡（注释：keep protocol tags fail-closed）。但这意味着该窗口内普通文本未经协议校验即发出。A-03 已加固标签识别，剩余风险取决于模型输出是否仍可能构造出绕过模式的内容 |

---

## 四、已审查维度（结论：未发现缺陷）

| 维度 | 范围 | 结论 |
|---|---|---|
| 前端 `innerHTML` | `webui/**/*.js` 共 146 处 | **0 违规**。66 处清空、35 处静态图标（`HZ.icon`/svg）、45 处静态字符串。逐条确认所有变量插值均为 `HZ.icon`；`context.js:340` 分页器的 `html` 仅由页码数字与静态 SVG 拼成。符合 AGENTS.md「禁止把持久化内容拼进 innerHTML」 |
| 发送闸门状态机 | `main.py:2868-2920` | 7 个 `EventState` 全部有分支，无 fallthrough 丢消息 |
| 工具阶段媒体消息 | `main.py:3287` | 无 `Plain` 组件时直接发送（纯媒体无需文本校验），合理 |
| 异常吞没（抽样） | `provider_catalog.py:53,223`、`snapshots.py:403`、`image_cache.py:83,96` | 均为「多策略回退 / 跳过坏条目」的降级设计，且大多带 `logger.exception`，非静默 |

## 五、审查日志

- 02:55 建立台账，完成首轮 AST 扫描（47 项初筛）
- 复核变量遮蔽 10 项 → 全为良性（N-01~N-04）
- 复核索引取值 → 发现 **A-01**（LIKE 通配符未转义），已修 `21b41bb`
- 复核 `except Exception` → 发现 **A-02**（索引失败仍报 cached=True），已修 `d966b56`
- 扫描前端 `innerHTML` 146 处 → **0 违规**，符合安全要求
- 审查协议 fail-closed 路径 → 发现 **A-03**（正则漏 `<Messages>`），已修 `429de73`
