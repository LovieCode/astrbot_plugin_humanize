# Bot 拟人化插件实施计划

## 1. 项目定位

`astrbot_plugin_humanize` 用于让 Bot 在长期交互中形成稳定、连贯、可解释的人格、状态、表达和行为选择，而不是只依赖一段静态 system prompt 模仿角色。

当前阶段先完成可审计、可配置、可逐步接入运行时的基础设施。关系记忆涉及作用域、隐私、衰减和冲突处理，暂不进入实现。

## 2. 已实现能力

### 2.1 回复控制协议

- 当前用户消息使用 `<Msg>` 作为不可信数据边界。
- 已知黑话使用 `<KnownTerms>` 注入，不和用户原文混写。
- 完整协议默认每轮临时注入用户消息末尾，避免只依赖 system prompt。
- `protocol_injection_mode=user` 仅做用户级临时注入；`both` 额外在 system prompt 保留副本。
- 最终文本使用轻量控制头，不需要 XML 根节点：

```text
Action: Reply
UnknownTerms: []
---
这里是普通回复正文。
```

- `Action` 当前只允许 `Reply` 和 `No Reply`。
- `UnknownTerms` 是单行 JSON 数组，插件校验后才会用于黑话学习。
- 插件发送前移除控制头，用户只看到普通正文。
- 控制头缺失、字段非法或 Action 不受支持时，最终文本会被发送闸门拦截。
- 工具调用本身不要求控制头；工具阶段如果需要展示附带文本，该文本仍必须经过同一 Action 闸门。
- 严格协议启用时关闭流式直出，避免未经完整校验的文本提前发送。

### 2.2 内容长度策略

- `max_message_chars` 只用于提示模型保持日常闲聊简短。
- 回复正文没有程序级字符数上限。
- 不限制独立消息数量，因为当前协议只保留一个完整普通文本正文。
- 代码、日志、命令、教程、引用和结构化数据按任务需要保持完整，不拆分、不截断。
- 协议日志可以截断保存原始输出用于诊断，但这不影响实际回复正文。

### 2.3 黑话学习

- 从合法 `UnknownTerms` 中接收候选词、推测含义、置信度和上下文依据。
- 排除普通词、人名、链接、随机数字和未出现在当前用户消息中的幻觉词。
- 按私聊或群聊作用域保存证据，避免不同会话互相污染。
- 下一次同一作用域命中已学习词条时，通过 `<KnownTerms>` 注入含义。
- WebUI 支持查看、筛选、确认、拒绝和修正词条，并展示协议审计日志。

### 2.4 管理与持久化

- Persona、State、Behavior、Expression 和 Control 已有统一的 WebUI 管理入口。
- 所有模块共用数据目录中的唯一 `humanize.db`。
- 所有读写共用同一 Repository、迁移链和连接路径。
- 禁止为单个 Agent、功能模块或页面创建独立数据库。
- 配置写入数据库后可在 WebUI 继续编辑；尚未接入运行时的功能必须明确展示状态，不伪造已生效。

## 3. 当前模块状态

| 模块 | 管理面 | 运行时 | 说明 |
| --- | --- | --- | --- |
| 回复控制 | 已完成 | 已完成 | 每轮临时注入、严格解析、发送闸门和审计 |
| 黑话词库 | 已完成 | 已完成 | 作用域学习、注入、审核和证据查看 |
| Persona | 已完成 | 待接入 | 稳定身份、特征、价值倾向和边界 |
| State | 已完成 | 待接入 | 情绪、精力、兴趣、压力和当前关注点 |
| Behavior | 已完成 | 待接入 | 回复、追问、主动发言和话题收束配置 |
| Expression | 已完成 | 待接入 | 表达画像、模式和外部风格插件集成 |
| Control | 已完成 | 部分完成 | 审计、恢复默认值和模块运行状态 |
| Relationship Memory | 研究中 | 未实现 | 暂不创建表、接口、页面数据或独立数据库 |

## 4. 运行链路

```text
用户消息
  -> 构建可信 MessageContext
  -> 用 <Msg> 包装用户原文
  -> 查询当前作用域的 KnownTerms
  -> 每轮临时注入 Rule、KnownTerms 和回复控制协议
  -> LLM / 工具循环
  -> 解析 Action 与 UnknownTerms 控制头
  -> 记录协议审计和有效黑话证据
  -> Reply: 移除控制头后发送完整普通正文
  -> No Reply: 保留用户历史但不发送文本
  -> 非法控制头: 清空结果并阻止泄漏
```

运行时接入 Persona、State、Behavior 和 Expression 后，扩展链路为：

```text
MessageContext
  -> Persona 提供稳定约束
  -> State 提供有时间性的当前状态
  -> Behavior 决定允许的行为和阈值
  -> Expression 生成表达约束
  -> 合并为本轮临时用户级上下文
  -> 继续执行现有回复控制和发送闸门
```

## 5. 下一阶段

### Phase A：Persona 与 State 运行时接入

目标：让已经保存的 Persona 和 State 真正参与每轮请求，同时保持可解释和可回滚。

实现内容：

1. 在请求准备阶段从统一 Repository 读取 Persona 与 State。
2. 以临时用户内容注入当前轮次，不把动态状态永久堆入 system prompt。
3. Persona 只提供稳定身份、价值倾向、边界和表达原则。
4. State 只提供当前情绪、精力、兴趣、压力和关注点。
5. 状态值缺失或读取失败时使用明确默认值，不能阻断基础回复链路。
6. WebUI 显示运行时接入状态和最后生效时间。

验收标准：

- WebUI 保存后，下一轮请求能读取对应配置。
- 注入内容可在测试中精确断言，不包含其他作用域数据。
- 关闭模块后不再注入该模块内容。
- 数据库异常不会绕过发送闸门，也不会创建备用数据库。

### Phase B：Behavior 决策执行

目标：把 Behavior 配置接入真实决策流程，而不是只保存表单。

实现内容：

1. 接入模块总开关和 `allow_no_reply`、`allow_follow_up`、`allow_proactive`、`allow_end_topic`。
2. 使用已配置阈值约束回复、追问、主动发言和结束话题的候选行为。
3. 主动发言仅在 `allow_proactive=true` 时启用阈值和冷却时间。
4. 所有主动行为必须经过 AstrBot 已有调度能力、权限判断和取消路径。
5. 当前回复控制协议仍只执行已注册 Action，禁止模型自行解释新行为。

验收标准：

- 禁用的行为无法被模型输出或旁路逻辑触发。
- 阈值和冷却值与 WebUI、API、数据库默认值一致。
- `No Reply` 继续保留用户消息历史。
- 未接入调度器前，主动发言明确显示为未生效，不创建假任务。

### Phase C：Expression 运行时与风格插件集成

目标：在不改变普通文本输出格式的前提下，控制表达画像并接入已有风格学习插件。

实现内容：

1. 将 Expression 的启用状态、provider、mode 和 profile 接入请求准备阶段。
2. 明确 builtin 与外部 provider 的能力边界和降级行为。
3. 外部插件不可用时记录 `integration_status` 和错误，不阻断基础回复。
4. 表达约束不得覆盖 Persona 边界、管理员权限或 Action 发送闸门。
5. 保持长代码、日志和结构化输出完整，不用风格规则改写任务内容。

验收标准：

- provider 切换可审计并能恢复默认值。
- 外部插件缺失、超时或返回异常时稳定降级。
- 用户实际收到的内容仍是无控制头的普通文本。

### Phase D：运行控制与可观测性

目标：让管理员能判断哪些配置已保存、哪些已生效、哪些仍等待运行时接入。

实现内容：

1. 补充模块启用状态、最后应用时间和最近错误。
2. 审计记录保存、重置和运行时应用事件。
3. 恢复默认值必须按模块执行，不影响其他模块和黑话数据。
4. 增加协议成功率、Action 分布和失败原因统计。
5. 对数据库迁移和配置兼容增加启动自检。

## 6. Relationship Memory 研究门槛

关系记忆暂不实现。开始编码前必须先完成以下设计：

1. 明确私聊、群聊、用户和跨群身份的作用域模型。
2. 明确哪些内容允许长期保存，哪些内容必须拒绝或脱敏。
3. 设计证据来源、置信度、时间衰减、冲突合并和人工修正机制。
4. 定义删除、导出、重置和用户隐私控制。
5. 明确多人群聊中关系信号的归属，避免把他人陈述写到错误用户。
6. 评估是否需要新表；即使新增表，也必须继续使用同一个 `humanize.db` 和 Repository 连接。

研究结果通过评审前：

- 不创建关系记忆数据库或独立连接。
- 不创建虚假的关系记忆数据。
- WebUI 只显示“规划中”，不提供看似可用的编辑能力。
- 不把普通聊天内容自动写成长期关系事实。

## 7. 配置原则

| 配置 | 默认值 | 约束 |
| --- | --- | --- |
| `enabled` | `true` | 关闭后不注入、不拦截、不学习 |
| `default_rule_enabled` | `true` | 每轮临时注入可信 Rule |
| `max_message_chars` | `10` | 仅为日常闲聊软建议 |
| `protocol_enabled` | `true` | 启用回复控制和发送闸门 |
| `protocol_injection_mode` | `user` | `user` 每轮临时注入；`both` 额外保留 system 副本 |
| `protocol_version` | `1` | 当前控制头协议版本 |
| `protocol_log_retention_days` | `7` | 诊断日志保留时间 |
| `no_reply_enabled` | `true` | 是否允许 `No Reply` |
| `jargon_enabled` | `true` | 是否学习合法 UnknownTerms |
| `min_confidence_for_injection` | `0.75` | 黑话进入注入链路的最低置信度 |
| `max_injected_jargons` | `5` | 单轮 KnownTerms 条数预算 |

不再提供：

- 回复正文字符数硬限制。
- 单次回复消息数量硬限制。
- XML 根节点、节点数、嵌套深度或 XML 输出长度配置。

## 8. 测试要求

### 协议与发送

- 合法 `Reply` 只发送 `---` 后的完整普通正文。
- 合法 `No Reply` 不发送文本，但保留当前用户历史。
- 缺失控制头、非法 Action、坏 JSON、字段缺失和非法置信度全部拦截。
- 长代码、日志、Markdown 和结构化数据保持字节内容与换行结构。
- 工具调用继续执行，工具阶段附带文本仍经过 Action 闸门。
- `user` 与 `both` 两种注入模式分别有测试覆盖。

### 数据与作用域

- 所有模块使用同一个 `humanize.db`。
- 私聊和群聊数据不会跨作用域注入。
- 配置保存、重启读取、恢复默认和迁移均有覆盖。
- 协议失败输出不会贡献黑话证据。
- 模块异常不会自动创建其他数据库。

### WebUI

- API、表单字段和后端默认值一致。
- 桌面与移动视口无文本遮挡和非预期横向溢出。
- Persona、State、Behavior、Expression、Control 可加载、编辑和保存。
- Relationship Memory 始终显示研究/规划状态。
- 名称修改必须单独讨论和确认，不随视觉重构自动应用。

## 9. 完成定义

每个阶段完成时必须同时满足：

1. 代码、配置、API 和 WebUI 使用同一数据契约。
2. Python 测试、Ruff、JavaScript 语法检查和静态契约测试通过。
3. WebUI 经过桌面和移动端实际截图检查。
4. 数据库迁移可重复执行，并继续使用唯一 `humanize.db`。
5. 运行时行为可审计、可关闭、可恢复默认值。
6. 文档描述与实际行为一致。
